# 1. Force pip to use the PyTorch index for CPU-only packages
--index-url https://download.pytorch.org/whl/cpu
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
# Align with albumentations 2.x (requires pydantic>=2.9.2)
pydantic==2.10.4
opencv-python-headless==4.9.0.80
mediapipe>=0.10.0
# 3. ML & CV libraries (optimized for size & Python 3.12)
# PyTorch 2.2+ is required for Python 3.12 support
torch==2.3.1+cpu
torchvision==0.18.1+cpu
timm==0.9.16
Pillow==10.3.0
# Compatible with mediapipe (<2.0) and OpenCV (>=1.23.5 on py>=3.11)
numpy==1.26.4
